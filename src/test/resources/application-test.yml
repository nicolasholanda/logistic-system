spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  security:
    username: admin
    password: admin

logging:
  level:
    root: WARN
    com.logistic: INFO
